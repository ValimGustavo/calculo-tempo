export function getClock() {var timer = new Date();var hours = timer.getHours();var minutes = timer.getMinutes();var seconds = timer.getSeconds();return { hours: hours, minutes: minutes, seconds: seconds };}export function transformInSeconds(time) {var timerSplitted = time.split(":");var hours = Number(timerSplitted[0]) * 3600;var minutes = Number(timerSplitted[1]) * 60;var seconds = 0;if (timerSplitted.length === 3) {seconds = Number(timerSplitted[2]);}var total = hours + minutes + seconds;return total;}export function takeTimeBetweenTwoMoments(firstMoment, secondMoment) {return secondMoment - firstMoment;}export function getAllSecondsElapsed(times) {var oddTime = undefined;if (times.length % 2 != 0) {oddTime = times.pop();}console.log(times);var total = 0;for (var i = 0; i <= times.length - 2;) {total += takeTimeBetweenTwoMoments(times[i], times[i + 1]);i = i + 2;}if (oddTime != undefined) {total += takeTimeBetweenTwoMoments(times[times.length - 1], oddTime);}return total;}//# sourceMappingURL=timer.js.map